const isSearch = state => state.isSearch;
const isCurrent = state => state.isCurrent;
const isStart = state => state.isStart;
const weather = state => state.weather;
const place = state => state.place;
export {
    isSearch,
    isCurrent,
    isStart,
    weather,
    place
};
